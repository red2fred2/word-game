/**
 * Keeps track of game state and information
 * @packageDocumentation
 */

/**
 * Keeps track of game state and information
 */
export class Game {
	constructor(size: number) {
		// console.log(size);
	}
}
